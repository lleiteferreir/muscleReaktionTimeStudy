using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Valve.VR.InteractionSystem;

public class VrCameraPosition : MonoBehaviour
{
    void Start()
    {
        //changed!!!
        /*Player player = this.gameObject.GetComponent<Player>();

        // Set camera rotation to initial player rotation
        this.gameObject.transform.Rotate(0, this.gameObject.transform.rotation.eulerAngles.y - player.hmdTransform.rotation.eulerAngles.y, 0);
        // Delta between VR camera position and player position
        this.gameObject.transform.position -= player.hmdTransform.position - this.gameObject.transform.position;
        */
    }
}
